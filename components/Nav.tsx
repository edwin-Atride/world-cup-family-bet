import Link from 'next/link';
import { Trophy, Home, User, Shield } from 'lucide-react';
export function Nav({isAdmin=false}:{isAdmin?:boolean}){ const items=[['/paris','Paris',Home],['/classement','Classement',Trophy],['/profil','Profil',User]] as const; return <nav className="fixed bottom-0 left-0 right-0 z-50 safe-bottom bg-fifaBlue/90 backdrop-blur border-t border-white/10"><div className="mx-auto max-w-3xl grid" style={{gridTemplateColumns:`repeat(${isAdmin?4:3},1fr)`}}>{items.map(([href,label,Icon])=><Link key={href} href={href} className="py-3 flex flex-col items-center gap-1 text-xs text-white/85"><Icon size={20}/>{label}</Link>)}{isAdmin&&<Link href="/admin" className="py-3 flex flex-col items-center gap-1 text-xs text-fifaGold"><Shield size={20}/>Admin</Link>}</div></nav> }
